# FastAPI AI Post Generator

API desarrollada con FastAPI para generar contenido de redes sociales usando IA.

## Tecnologías
- FastAPI
- PostgreSQL
- JWT Authentication

## Cómo ejecutar

```bash
pip install -r requirements.txt
uvicorn main:app --reload